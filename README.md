# README

새로운 브런치 생성하기

```cmd
git checkout stg
git pull origin stg
git checkout -b feature/<이름>-<A or R>-<sequance>
```

---

stg 머지하기

```cmd
git checkout stg
git merge feature/<이름>-<A or R>-<sequance> --no-ff
```

## EXAMPLE 1
